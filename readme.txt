Connect.php is used for connecting with database

Index .php refer to registration of users and vendors , a class register is used you have to select user or vender than according to role users will be registered in database .

User/login.php is for login of user 

Vendor/login is for vendors login 

Vendor/list.php is used for displaying products of vendors it will pick products using vendor id in database, it will get id when vendor will login there .

Normal registration and login forms  are also attached for sample.